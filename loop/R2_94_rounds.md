# 轮次台账 · jagent

## R1 · 2026-09-19 · 首轮：可运行 demo

**触发**：`R1_00_req_product.in.txt`（作者愿景全文）。

**本轮着力点**：把愿景落成一个能在终端跑起来、且**设计理念可被肉眼观察**的 demo。

**范围**

- 内：ReAct 主循环与工具、终端 Unicode 图渲染、多 agent 并行、记忆与语义压缩、
  博弈论（密封拍卖选模型档位）、经济学（预算账本 + 压缩 ROI）、控制论（并发闭环与熔断）
- 外（推迟 R2）：进化论基因组、GraalVM native、分层流程图布局、终端 raw mode 行编辑

**关键决策**

| 决策 | 结论 | 理由 |
|---|---|---|
| 依赖 | 纯 JDK 零依赖 | native 编译、体积、启动速度的前提 |
| 并发 | 不用 `StructuredTaskScope` | Java 25 仍是 preview，锁死版本且拖累 native |
| 上下文 | `ScopedValue`（JEP 506 已转正） | 工具签名无需透传上下文，Java 相对 Go 的优势 |
| 离线可测 | `MockClient` 列为必需 | 无 key 也能演示，否则每次演示烧 token 且无法自测 |
| 流程目录 | 过程文件收进 `loop/` | 偏离 skill 的根目录平铺，理由见任务书 §流程适配 |
| 语言 | 两层：L1 内部固定英+缩略；L2 面向用户首启选 Zh/Eng | 提示词 `[]` 里是可选项而非常量；缩略只在英文成立 |

**本轮中途回溯（第 1 次）**

| 项 | 内容 |
|---|---|
| 触发 | 作者指出：提示词 `[]` 中的语言是**用户可选项**，不该写死成中文 |
| 判定 | 属回溯矩阵"口径/范围变了" → 退 **S1** 重建任务书口径 |
| 实际做法 | **增量口径，不推翻已建件**：P0/P1 产出全部保留可用，只在其上新增语言层 |
| 波及 | 新增 `R1_30_design_lang_slots.wip.md`；改 `R1_90` #5 + §3.5、`R1_10` §2、本表；产品侧新增 `Lang`/`lang/*.properties`/`system.txt` 模板化 |
| 未动 | `R1_03_style_prompt.in.txt`（只读输入原稿，铁律） |

**交付物**：见 `R1_90_req_constitution.md` §4。

**G3 送审方式**：由作者在本机终端实跑 —— 图渲染是否花屏只有真实终端能验证。

**G3 结果（2026-09-19，已回填）**：**通过**。

作者在本机真终端实跑 `doctor` / `run fanout` / `run fanout --compress-after 64` / `--resume`，
确认底部执行图**原地覆盖刷新**（`Dashboard` 的 `up(n)+clearDown` 生效，非滚动追加）、
树形连接符与状态字形整齐、中英混排宽度对齐无错位、并发与竞价与压缩均正常。

> 首跑乱码事件：输出被抄屏后观感"乱七八糟"。诊断结论为**终端环境问题而非产品缺陷**——
> cmd 代码页 cp936 把程序 UTF-8 字节按 GBK 解码，造成中文双重编码；且抄屏丢失光标控制转义，
> 使 217 次原地重绘被堆叠成 1493 行树。`doctor` 自身早已报出 `stdout 编码 ms936 / 控制台 GBK`
> 并给出 `chcp 65001` 处方。切 UTF-8 复跑即恢复正常。**产品侧零改动，266 断言基线不受影响。**

**G4 结果（2026-09-19，已回填）**：**通过 — 定为终稿，只建本地仓库，不推远程。**

| 项 | 值 |
|---|---|
| 仓库 | `C:\Users\lenovo\Desktop\jagent`，`git init -b main` |
| 交付基线提交 | `a8761b4` R1 交付（**R2 的起点，只读**） |
| 补充提交 | `2a1ca57` gitignore 修正 |
| 记录归档提交 | `8a625fa` / `f36f3e3` / `7678a05`（meta，滚动，不属交付基线） |
| 署名 | Lancelot \<higerjoth1@foxmail.com\>，`-c` 临时传入，未改 git 配置 |
| 入库 | 68 文件；排除 `target/`、`work/`、`demo-run/`、`.claude/settings.local.json` |
| 远程 | **无**（本轮明确不发 GitHub） |

定稿前对账：宪法 §4 六项交付物齐备；全库无密钥；`.mvn/wrapper` 无 jar 故不受 `*.jar` 忽略影响。
交付物 md5 已记入 `R1_93_frozen.md`。

**进展（2026-09-19 续）**：P0–P5 全部完成，断言累计 **266/266 全绿**
（P1T 26 / P2T 27 / P3T 32 / P4T 61 / P5T 120）；`README.md`、`docs/PROTOCOL.md`、
`docs/DEMO.md`、`loop/R1_50_bench_startup.wip.md` 已建。期间修 3 处缺陷
（L1 语言残留 2 处、压缩进度累计 1 处，详见 `R1_92_logs.txt`）。

**未完成**：无阻塞项。G3 通过、G4 通过、`git init` 已执行。远程发布不在本轮范围。

**回溯记录（第 2 次）：目录改名**

| 项 | 内容 |
|---|---|
| 触发 | 作者将项目目录 `agent` 手动改为 `jagent` |
| 判定 | **不属口径/范围变化** —— G1 早已锁定名称为 `jagent`，改名是使目录**与口径一致**，方向为修正 |
| 实际做法 | **不退 S1**，已建件全部保留；全库检索旧绝对路径仅命中 Maven 增量编译缓存一处，删除后重建即按新路径 |
| 波及 | `target/maven-status/...`（已清理并重建）；重跑 P1T–P4T 确认基线 146 全绿未受影响 |
| 未动 | 源码、`pom.xml`、`loop/`、`.claude/` —— 均无旧名或绝对路径引用 |

**本轮拍板（2026-09-19）**

| 事项 | 决定 | 理由 |
|---|---|---|
| 压缩摘要逐轮累积（`Compressor.digest` 并入上一版摘要） | **留 R2 修** —— R2 又推迟至 R3，见 R2 台账 | 短任务无感；改压缩语义需重验五套断言 |
| `git init` 时机 | **G4 通过后再做** —— 已于 2026-09-19 G4 通过后执行 | 定稿前不产生版本历史，符合原计划 |
| 发布方式 | **只建本地仓库，不推远程** | 方案尚未经公开检验，先自留可回溯版本 |

**R1 收尾状态**：定稿完成，交付基线 `a8761b4`。

---

## R2 · 2026-09-19 · 双厂商接入与拍卖机制真实验证

**触发**：`R2_00_req_product.in.txt`（作者口头要求：配两个模型以测试拍卖机制，先跑通最简 agent）。

**判定**：属回溯矩阵的**范围推进**（新增外部真实模型接入），不是对 R1 的不满意意见
→ 不退 S1 重做 R1，直接在基线 `a8761b4` 上叠加。

**本轮着力点**：让两档模型第一次来自**真正不同的厂商**，使密封拍卖面对真实的实力差异，
而不是同一张性格表自己跟自己竞价。

**范围**

- 内：双档位独立 `base_url` / `api_key` / `model`；档位化客户端工厂；
  最简 agent 真实厂商跑通；一次真实竞价并固化证据；相应断言
- 外（本轮明确不取，作者原话"把你本来预设 R2 的东西推迟"）：压缩摘要自引用累积修复、
  进化论基因组与变异算子、GraalVM native 与 RSS 基准、分层流程图布局、raw mode 行编辑

**关键决策**

| 决策 | 结论 | 理由 |
|---|---|---|
| 厂商归属 | **由作者自定，产品不得预设**（新增硬约定 #7） | 接入哪家是部署决策而非产品特性；写死厂商会让"两档竞价"退化为"两家特定厂商竞价" |
| 厂商身份留存 | **不写入仓库任何文件**（含 `loop/`） | 属作者部署私事 |
| 密钥投喂 | 作者自行写入 `work/jagent.properties`（已 gitignore） | 满足硬约定 #4；且使 Claude 可代为运行而不接触密钥 |
| 宪法维护 | 采**修订页**（`R2_90`）而非整份复制 | 整份复制会产生两份口径，只改其一会造成矛盾裁定依据 |
| `CLAUDE.md` | 作者已删除，冲突随之消除 | 其语言条款硬编码 Zh，与硬约定 #5 冲突 |

**阻塞点**：`Cli.java:86-90` 对两档共用 `baseUrl` + `apiKey`，`Config.java:77` 仅提供强档模型名
→ 现状只能"同厂商两模型名"竞价。需增强档地址与密钥。

**交付物**：见 `R2_10_task_round.wip.md` §3。

**进展（2026-09-19）**：S2–S5 完成（可自主部分）。

| 阶段 | 结果 |
|---|---|
| P0 双档位配置 | `Config` 增 `baseUrlStrong`/`apiKeyStrong` + `strongBaseUrl()`/`strongApiKey()`/`hasStrong()`；留空回退廉价档 |
| P0 厂商无关 | `base-url` 默认由 `https://api.openai.com/v1` 改为 `""`，落实硬约定 #7；`model` 默认保留为离线展示标签 |
| P1 客户端工厂 | `Cli.client(cfg,url,key,model)` 按档取用；`run()` 强力档改用 `cfg.strongBaseUrl()/strongApiKey()` |
| P2 快速失败 | 新增 `badCfg()`：非 mock 且 url/key 缺失时立即报错列出缺失项，不再等到 API 调用才撞墙 |
| P3 展示 | `doctor` 四行档位 + 同源黄字提示；`help` 增 `--base-url-strong`/`--api-key-strong` |
| P4 语言键 | zh/eng 各增 9 键、删 3 死键（`col.baseurl`/`col.model`/`col.apikey`），键集合严格对称 |
| P5 断言 | 新增 `P6T` 16 组 79 条；`P1T`–`P5T` 回归 266 条 → **345 全绿** |
| P6 自测 | `run fanout` mock 端到端 4 步 26 节点通过；`--model`/`--model-strong` 双标签正确 |

**口径追加（2026-09-19）**：作者要求本轮不止验证拍卖，而要"像一个能用的 agent"
把 R1 的四组机制**全部测一遍** → 按回溯矩阵"口径变了"退 **S1** 重建口径
（`R2_90` 修订 2b；`R2_10` §2/§3/§8）。做法是新增**机制对账单**：计数在各机制类原地累加，
`run` 收尾汇总成一块并落盘 `work/scorecard.txt`，使四组机制能在**一次运行**里对账。

| 新增 | 内容 |
|---|---|
| `agent.Scorecard` | 9 行对账（拍卖/信誉/预算/压缩/控制论/路径互斥/降级/图），中英双语 |
| 计数 | `Auction.wins`；`Governor.ups/downs/trips`；`Admission.peak`；`FallbackClient.fallbacks/retries` |
| 落盘 | `WorkDir.SCORECARD` = `work/scorecard.txt` |
| 语言 | zh/eng 各新增 10 键，键集合仍严格对称 |
| 断言 | `P7T` 109 条；全量 **454** 条全绿（26/27/32/61/120/79/109） |
| 电池 | `R2_95_mech_battery.sh` 五场景 → `R2_50_mech_evidence.wip.txt` |

**机制验证结论（mock 证据）**

| 机制 | 证据 | 判定 |
|---|---|---|
| 博弈论 拍卖 | 基线强力档 10:0；加预算压力后 5:5 | 活，随压力改变选择 |
| 博弈论 信誉 | 故障注入后强力档 1.00 → 0.75 | 活 |
| 经济学 预算 | 226% 与 6% 两个极端均正常记账 | 活 |
| 经济学 压缩 ROI | 低阈值下 3 次压缩、省 518 tok；默认阈值 0 次 | 活 |
| 工程经济学 利润 | +1.38 与 -1.23（预算压力下转负） | 活 |
| 控制论 闭环调参 | 起手 1 许可，上调 7 次至 8，并发峰值 6 | 活 |
| 控制论 熔断 | 故障注入下熔断 1、下调 2、错误率 49% | 活 |
| 图工程 | 26 / 29 / 2 节点三态均正确 | 活 |
| 路径互斥 | 五场景均 0 冲突（mock 不并发写同一路径） | 仅单测覆盖 |

**发现（转 R3）**：熔断 `tripAt=3` 与"模型调用失败即中止整轮 + `maxAttempts=2`"组合，
使单轮内最多累计 2 次失败，**跨厂商自动切换在一次运行内无法自然触发**；
当前熔断只对"多次调用的长任务"生效。

**真实双厂商跑通（2026-09-20）**

作者投喂两档真实配置（写入 `work/jagent.properties`，已 gitignore；厂商名按硬约定 #7 不入库）。
五个场景 d1–d5 全部实跑，证据件 `R2_51_real_vendor_evidence.wip.txt`，日志 `demo-run/d*.log`。

| 场景 | 结果 |
|---|---|
| d1 基线 | 3 步 6 节点，强力档中标，写文件读回成功 |
| d2 预算压力 | **拍卖真翻转**：轮1 强力档，轮2/轮3 廉价档中标；两档各自真实执行工具调用 |
| d3 并发 | 6 步 37 节点（完成 31 / 失败 6）；降级 12 重试 2；熔断 1 |
| d4 压缩 | 1 次压缩，省 211 tok，ROI 1743.5x |
| d5 故障注入 | 厂商限流 429 → 重试 1、熔断 1、强力档信誉 1.00→0.75、中止本轮 |

**本轮修掉的三个缺陷**（均由真实厂商实跑暴露，mock 不复现）

| # | 缺陷 | 修法 |
|---|---|---|
| 1 | 思维链模型多轮工具回合缺 `reasoning_content` 回传 → http 400 | 客户端捕获流式 `reasoning_content`；`Turn`/`Message` 增分量；非空才回传。P1T +4 断言 |
| 2 | `Admission.setLimit` 降额时阻塞等待"正由调用者自己持有"的许可 → 死锁 | 降额改为只回收当前可用许可，不再阻塞；`ups/downs` 只记实际变化 |
| 3 | 嵌套许可死锁：持许可的 `spawn_agent` 在等需要许可的子 agent 工具调用 | 许可只约束顶层提交，嵌套提交不再重复申请 |

**副作用（需在下轮留意）**：修复 3 使"并发峰值"只统计顶层提交，mock D 峰值由 6 降为 2。

**G3 结果（2026-09-20，第二回）**：**通过**。

作者本机终端复核 d1–d5：执行图渲染与"机制对账"块观感正常，四组机制判定与记录一致。
首回送审暴露的 3 处缺陷已退回 S5 修完（见上表），本回为修后重送。

复核时另提**四项新意见**（均属显示与交互层，不影响机制正确性）：

| # | 意见 | 诊断 |
|---|---|---|
| 1 | 执行图节点摘要显示不全，末句被切 | `Agent.summarize` 先截 60 字符，`GraphView.clip` 再截 48 字符，二次截断叠加 |
| 2 | `chat` 里灰色思维链与答案黏在同一行 | 推理增量与答案增量都只 `print`，谁都不打换行 |
| 3 | 没有可反复敲命令的输入界面 | 即 R1 宪法 §5 明确推迟的"终端 raw mode 行编辑" |
| 4 | 机制标签带明确学科色彩（竞价/拍卖/信誉/利润/预算/控制论…） | 这批标签被错置于 L2（随用户语言变），按硬约定 #5 应属 L1 |

**G4 结果（2026-09-20）**：**通过 — 定为 R2 终稿。**

| 项 | 值 |
|---|---|
| 冻结决定 | 作者拍板：**四项新意见不并入 R2，整体转 R3**；R2 保持首回送审时的形态 |
| 交付基线提交 | 见 `R2_93_frozen.md` |
| 署名 | Lancelot \<higerjoth1@foxmail.com\>，`-c` 临时传入，未改 git 配置 |
| 远程 | 仍**无**（沿用 R1 的"只建本地仓库"决定） |

**本轮收尾状态**：定稿完成。R2 的关键资产是"四组机制在真实厂商下同时活着"的可复现证据
（`R2_50`/`R2_51`），以及真实实跑才暴露的 3 处缺陷修复。

**R3 首批待办**

本轮送审带回的四项（优先级最高，作者已给出口径，见 `R3_00_req_product.in.txt`）：

0. 执行图节点摘要改为**树形图只画状态、末尾统一给一段总结**（不再在节点上塞长摘要）。
1. `chat` 推理与答案之间加换行（思维链可整体另起一行）。
2. 新增 **REPL**：像 shell 一样反复敲命令并回显（R1 推迟的 raw mode 行编辑）。
3. 机制标签**中性化**：只改终端显示文字（不动代码类名/键名/文档），只换最带学科色彩的少数几个。

承接 R1/R2 未取项：

4. 决定熔断与中止策略的关系（`tripAt=3` 与"失败即中止整轮 + `maxAttempts=2`"组合，见"发现"）。
5. 子 agent 工作区隔离（d3 中两子 agent 互相读到对方产物，独立性不成立）。
6. 压缩摘要不得自引用累积（摘要只并入事实，不并入上一版摘要）。
7. 进化论基因组与变异算子。
8. GraalVM native 构建，并补 native 启动/体积数据与**常驻内存 RSS** 基准。
9. 分层流程图布局。
