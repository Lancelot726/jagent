# 轮次台账 · jagent

> 本文件只记 **R4**。R1 / R2 / R3 的台账原文见各自轮的 `R1_94_rounds.md` / `R2_94_rounds.md` / `R3_94_rounds.md`
> —— 按本项目约定，进度图 / 流水 / 台账 / 冻结清单按轮新开，**不做跨轮复制**（复制会产生两份口径）。

## 前轮索引

| 轮 | 主题 | 定稿 | 台账 |
|---|---|---|---|
| R1 | 可运行 demo：骨架 / 图渲染 / 并行 / 记忆 / 压缩 | 2026-09-19，基线 `a8761b4`(+`2a1ca57`) | `R1_94_rounds.md` |
| R2 | 双厂商接入 + 四组机制真实验证 + 对账单 | 2026-09-20，基线 `996d71d` | `R2_94_rounds.md` |
| R3 | 终端交互与显示口径：REPL / 摘要呈现 / 标签中性化 | 2026-09-20，基线 `630f92b` | `R3_94_rounds.md` |

---

## R4 · 2026-09-24 · 说明文本去学科化

**定稿**：2026-09-24 经作者 G4 拍板冻结，交付基线 `2917c0c`（清单见 `R4_93_frozen.md`）。

**触发**：`R4_00_req_product.in.txt`（作者 2026-09-24 在 R3 定稿后直接下达）。

**判定**：属回溯矩阵的 **口径变化**，按矩阵退 **S1（重建口径）**。

| 意见 | 类型 | 按矩阵应退 |
|---|---|---|
| 说明文本去学科化 | **口径变了**（R3 划定的作用面边界被推翻） | S1（重建口径） |

`R3_90` 修订 1 曾明确"标签中性化只改终端显示文字，`README`/`docs/` 一律不动，
`loop/` 过程文件里的学科表述保留"。本条推翻该边界，把去学科化的作用面从显示层
扩到 `README` / `docs/` / `loop/` 全部说明性文本。故**整轮新开 R4**，不并入已冻结的 R3。

**本轮着力点**：让这个项目的**说明文本**读起来像一套成熟工程系统的架构说明，
而不是跨学科借词的堆叠。只改措辞，不动机制、不动代码。

**范围**

- 内：`README.md` / `docs/DEMO.md` / `docs/PROTOCOL.md` 措辞替换。
- 内：`loop/` 全部说明性文件（`*.md` / `*.txt` / 脚本注释）措辞替换。
- 内：`lang/{zh,eng}.properties` 三处用户可见文案（`opt.modelstrong` / `opt.scorecard` / `tier.sameSrc`）。
- 内：宪法 §1 项目定义改写（作者裁定"一并改写"）。
- 内：术语映射表（`R4_10` §3），承担未改动文件的旧新对应可追溯性。
- 外：**任何代码、类名、lang 键名、协议、断言语义** —— 本轮零改动。
- 外：**只读存档** `loop/*.in.txt`（作者原话）与逐字捕获的程序输出证据件
  （`R2_50_mech_evidence.wip.txt` / `R2_51_real_vendor_evidence.wip.txt`）—— 按「只增不改」保持原样。
- 外（承接，见文末）：R3 转出的六项待办。

**关键决策**

| 决策 | 结论 | 理由 |
|---|---|---|
| 轮次形式 | **新开 R4，走 S0–S8 全流程** | 口径变化须退 S1 重建，不并入已冻结的 R3 |
| 作用面 | README / docs / loop **全部说明文本** | 本轮即为此而开；推翻 R3_90 修订 1 的旧边界 |
| 只读存档怎么处理 | **保持原样，另建映射表** | 「只增不改」铁律 + 证据保真；可追溯性由映射表承担 |
| 证据件（`R2_50`/`R2_51`） | **保持原样** | 程序 stdout 逐字捕获，改之等于伪造证据 |
| §1 项目定义 | **一并改写** | 作者裁定；定义句本身含"方法论"与学科式枚举 |
| 用词风格 | **业界通用术语**（模型路由 / 预算记账 / ROI 判定 / 自适应并发控制 / 熔断器 / 执行图） | 作者指定的目标观感 |
| 「成本收益」 | **禁用** | 作者明确点名；换成 ROI 判定 / 投入产出对账 / 对比开销 |
| 熔断的写法 | 统一作 **熔断**（不再是"熔断触发"） | 与 `circuit breaker` 对齐，去名词冗余 |
| 代码与断言 | **不动**；语言包是断言数据源，故改后复跑全套 | 改的是说明文本，运行时行为零变化 |
| 「经济学」的上位词 | 取 **资源管理**（不取"预算记账"） | 与表内已用的"预算"并列时不重复 |

**阻塞点**：无。R4 已定稿冻结。

**交付物**（详见 `R4_10` §6）：措辞更新后的 README / docs / loop 说明文件、
`lang/{zh,eng}.properties` 三处文案、宪法 §1 改写（`R4_90` 修订 1）、术语映射表、R4 记录。

**验证**：残留扫描分两轮。首轮按映射表复扫；**第二轮换一张更宽的候选词表**
（学科名 + 生态 / 生物 / 市场 / 会计 / 控制理论等旁支）全库扫描，又揪出 4 处表外词
（基因组 / 变异算子 / 资产 / 出价），已一并收口。

两轮之后：§3 映射表全部借词 + 宽扫候选词，命中仅允许出现在
「保持原样」的 `*.in.txt` / `R2_50` / `R2_51`，以及 R4 记录本身（须引用旧词）；
其余说明性文件仅余「需求」（标准工程词，非学科借词）。
禁用词「成本收益」在说明文本与产品文案里 0 命中。
zh / eng 语言包键集合仍严格相等（各 94 键）。
lang 资源是断言的数据源，改动后同步进 `target/classes` 并复跑全套：
**666 全绿**（P1T 30 / P2T 27 / P3T 32 / P4T 61 / P5T 120 / P6T 79 / P7T 109 / R3T 208）。
本轮**未触碰任何 Java 源码**，故断言逻辑与机制行为零变化。

**进展**：**S0–S8 全部完成**，R4 已定稿冻结。G3 由作者 2026-09-24 以复核问答验收
（确认学科借词已清、源码零改动；本轮只动说明文本与语言包文案，未单独终端实跑）；
G4 作者裁定「可以，这一轮结束」，遂落交付提交 `2917c0c` 并写 `R4_93_frozen.md`。
下一轮（R5）待作者触发。

**沿用前轮的约束**（不得因本轮改动而破坏）

| 约束 | 来源 | 对本轮的要求 |
|---|---|---|
| 只读输入 `.in.txt` 只增不改 | R1 §3.5 唯一例外条 | 作者原话逐字件本轮不动 |
| 全库无厂商名 / 地址 / 密钥 | R2_90 修订 3（硬约定 #7） | 改后文本同样不得引入厂商名 |
| 机制标签归 L1（固定英文） | R3_90 修订 1 | 本轮改散文用词，不得反向把标签改回可选项中英 |
| 666 断言基线 | R3 收尾 | 本轮不动代码，断言逻辑不变 |

**承接（本轮明确不取）**

1. 完整版 raw mode 行编辑（方向键历史 / 行内编辑）。
2. 熔断与中止策略的关系（`tripAt=3` 与"失败即中止整轮 + `maxAttempts=2`"组合）；
   并补**步数不限时的第二道硬闸**（无进展即停 / 预算耗尽即中止）。
3. 子 agent 工作区隔离（R2 的 d3 中两子 agent 互相读到对方产物，独立性不成立）。
4. 压缩摘要不得自引用累积（摘要只并入事实，不并入上一版摘要）。
5. GraalVM native 构建，并补 native 启动 / 体积数据与**常驻内存 RSS** 基准。
6. 分层流程图布局。

**暂缓（无期限）**：自适应演化基因组与变异算子 —— 作者 2026-09-20「暂时删掉」。
