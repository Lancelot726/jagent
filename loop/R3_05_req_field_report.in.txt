R3 现场实跑问题单（第二回）· jagent
来源：作者 2026-09-20 在真终端实跑 R3 编码后交回（G3 现场第二次意见）
性质：**R3 的 G3 现场意见**，经作者当场裁定**并入 R3**（不新开 R4）
基线：R3 第一回现场意见（`R3_04`）已落地之后、步数上限改配置项（`R3_10` §2.3）已落地之后
与 `R3_04` 的关系：同一篇实录被通读后分两回下达 —— 第一回四项见 `R3_04`，本件为第二回

======================================================================
作者原话（逐字，未改写）
======================================================================

（先原样贴回终端里的一段，作为被指的对象）

  本轮总结
    任务  之前我让你做了什么
    工具  recall ×1
    结果  完成（用了 2 步）
    已记入 C:\Users\lenovo\Desktop\jagent\work\runs.md

（随后一句）

这个东西以后不要出现了 因为已经有answer部分了 非图像模式最好也能简单显示每一个step进度

======================================================================
判读（贴回的那一屏是什么）
======================================================================

它不是新的报错，而是 R3 第一回反馈【4】「要有总结段落」的**落地结果本身被否掉**：
`Cli.run` 收尾时在 `answer` 段之后又叠了一个四行块（键族 `rep.*`）。
同一屏上其实叠了四截：`answer` 段 → `[steps=...]` / `[quota ...]` / 计划已写入 →
蓝色 `summary` 机制对账八行 → 绿色「本轮总结」四行。作者要的是**收尾只留一处**。

======================================================================
Claude 对输入的三条归类判断（供作者确认，判断错请纠正）
======================================================================

| # | 原文指向 | 类型 | 根因（已定位） |
|---|---|---|---|
| 1 | 「这个东西以后不要出现了 因为已经有answer部分了」 | **呈现冗余**（同一件事写两遍） | `answer` 已承担收尾；`report()` 又按「任务 / 工具 / 结果 / 已记入」重说一遍 |
| 2 | 「非图像模式最好也能简单显示每一个step进度」 | **呈现澄清**（不是缺陷） | 过程行本来就有（`step` / `tool` / `result`）；冒犯感来自 `pick` 竞价行与整串原始 JSON 参数 |
| 3 | （无独立表述，属第 1 条的连带） | **呈现冗余** | 同屏第三个收尾块（蓝色 `summary` 八行）也属机制自证，日常视角不需要 |

三项**均不触碰 R2 已冻结的机制**（竞价 / 预算 / 信誉 / 控制论的行为与积分不变）；
`--scorecard` 关掉的只是**打印**，`work/scorecard.txt` 与 `work/runs.md` 的落盘照旧。

作者 2026-09-20 裁定口径（三条，详见 `R3_10` §2.4）：

- 【1】**绿色「本轮总结」块整体删除**，三种结局改由 `answer` 一段承担（失败也给「第几步 + 原因 + 建议」）
- 【2】**保留 `step` / `tool` / `result` 三类过程行**，去掉噪声：工具参数改摘要显示，原始 JSON 不上屏
- 【3】**机制对账块默认不开**，加 `--scorecard` 才显示

**本条同时推翻了 R3 自己的一条**：`R3_10` §2.2 追加 f 曾定「每轮末加运行总结（成功 / 失败都有）」，
现改为由 `answer` 段承担同一职责。目标（收尾必须说清结局）不减，载体变 —— 已记入 `R3_90` 修订 6 的修订说明。
